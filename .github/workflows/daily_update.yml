name: TEFAS Gunluk Guncelleme

on:
  schedule:
    # Türkiye saati (UTC+3, sabit) -> UTC karşılıkları
    # Yuvarlak dakikalardan kaçınmak için birkaç dakika kaydırılmış
    - cron: "3 6 * * 1-5"    # 09:03 TR
    - cron: "7 7 * * 1-5"    # 10:07 TR
    - cron: "33 9 * * 1-5"   # 12:33 TR
    - cron: "37 13 * * 1-5"  # 16:37 TR
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Bagimliliklari kur
        run: pip install -r requirements.txt

      - name: Veriyi guncelle
        run: python scripts/update_data.py

      - name: Puanlama sistemini calistir
        run: python scripts/score_funds.py

      - name: Degisiklikleri kaydet
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add tefas_gecmis_veri.parquet docs/
          git diff --quiet --cached || git commit -m "Otomatik guncelleme"
          git push
